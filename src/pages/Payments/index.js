import React, { useState } from 'react';
import { Button } from 'react-native';

import { Container } from './styles';

export default function Payments() {
  return (
    <Container>
      <Button title="conectar" onPress={() => {}} />
    </Container>
  );
}
